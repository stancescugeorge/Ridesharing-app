<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proiect">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_car"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_car"
        android:supportsRtl="true"
        android:theme="@style/temaPersonalizata">
        <activity android:name=".EditareStergereCursaActivity" />
        <activity android:name=".IstoricCurseActivity" />
        <activity android:name=".ListaCurseCautateActivity" />
        <activity android:name=".CautaCursaActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".OferaCursaActivity" />
        <activity android:name=".ContinutPrincipalActivity" />
        <activity android:name=".CreareContActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>